# 光线投射中的细节层次（LOD）控制方案与数学模型

在《Efficient Sparse Voxel Octrees》(laine2010tr1_paper.pdf) 一文中，光线投射的细节层次（LOD）控制本质上是**基于光线足迹（Ray Footprint）与体素物理大小的动态匹配**。

其完整的实现方案和数学模型可以分为核心判定公式、0次Bounce设定以及多次Bounce设定三个部分。

## 一、 核心数学模型与 LOD 判定公式

该方案将每一根光线视为一个带有发散角的“圆锥体”或“光束”，随着光线行进距离 $t$ 的增加，其截面大小会呈线性增长。在遍历八叉树时，当**光线在当前距离下的截面大小** 大于 **当前遍历到的体素物理大小** 时，停止细分遍历。

### 1. 体素尺度（Voxel Size）的数学表示
假设场景的根节点（Root）是一个边长为 1 的单位立方体。八叉树的深度最大层级设为 $s_{max}$。
当光线遍历到层级为 $scale$ 的节点时，该体素的边长（物理尺度）可以用 $2$ 的幂次方表示：
$$Size_{voxel} = 2^{scale - s_{max}}$$
在这个实现中，该值被预计算并存储在变量 `scale_exp2` 中。

### 2. 光线传播距离与交点
对于从原点 $p$ 发出，方向为 $d$ 的光线 $P(t) = p + t \cdot d$。
由于体素是轴对齐包围盒（AABB），光线离开当前体素包围盒的最大距离（即光线在当前体素内的最远穿出点）为 $tc_{max}$：
$$tc_{max} = \min(t_x(x_{exit}), t_y(y_{exit}), t_z(z_{exit}))$$
其中 $t_x(x) = (x - p_x) / d_x$，预计算逆向分量可以将其转化为乘加运算。

### 3. 光线足迹大小（Ray Footprint）
光线在距离 $tc_{max}$ 处的截面大小可以通过一个线性函数近似：
$$Size_{ray}(tc_{max}) = tc_{max} \cdot ray\_size\_coef + ray\_size\_bias$$
*   `ray_size_coef`：光线扩散系数（斜率），代表光线随距离发散的速度。
*   `ray_size_bias`：光线初始的截面大小（截距）。

### 4. LOD 提前终止条件
在光线与体素求交的循环内部，LOD 的截断判定公式为一个简单的不等式：
$$tc_{max} \cdot ray\_size\_coef + ray\_size\_bias > 2^{scale - s_{max}}$$
**解释**：如果上述不等式成立，说明当前体素的尺寸 $2^{scale - s_{max}}$ 已经**小于**光线在这个距离下覆盖的范围。此时继续向下遍历更小的子体素对最终画面的贡献不到一个像素，因此直接 `break` 终止遍历，返回当前父体素的数据进行着色。

---

## 二、 0次 Bounce（主光线/Primary Rays）的 LOD 参数设定

主光线（从相机发出的光线）用于渲染直接可见的表面。我们要确保一个体素在屏幕上的投影刚好约等于一个像素的大小，以避免锯齿（Aliasing）和性能浪费。

1.  **初始大小（Bias）**：由于光线是从相机的理想针孔（Pinhole）发出的，初始面积为 0。
    $$ray\_size\_bias = 0$$
2.  **扩散系数（Coefficient）**：它由相机的视场角（FOV）和屏幕的分辨率（Resolution）严格决定。代表单个像素所张开的立体角跨度。
    假设相机的垂直视场角为 $	heta_{FOV}$，屏幕高度为 $H$ 像素，则：
    $$ray\_size\_coef = rac{2 \cdot 	an(	heta_{FOV}/2)}{H}$$
    *(这意味着距离相机越远，光线足迹越大，不等式越容易成立，触发早期终止；屏幕分辨率 $H$ 越低，系数越大，LOD 截断越早，渲染出更低精度的体素)。*

---

## 三、 多次 Bounce（次级光线/Secondary Rays）的 LOD 参数设定

次级光线（阴影、反射、AO、全局光照等）是从物体表面（第一次交点）弹射出去的光线。这里的核心差异在于，次级光线**继承了主光线在交点处的足迹大小**，并根据材质的发散特性拥有新的**扩散系数**。

假设主光线在距离 $t_{hit}$ 处击中表面，准备发射次级光线：

1.  **初始大小（Bias）继承**：
    次级光线的起点不再是零大小，它的起点范围就是主光线击中表面时的范围。
    $$ray\_size\_bias_{secondary} = t_{hit} \cdot ray\_size\_coef_{primary} + ray\_size\_bias_{primary}$$
    *(这保证了次级光线一开始检索的体素层级就不会比表面交点处更精细，从而实现了“自动下采样 / automatic downsampling”)。*

2.  **扩散系数（Coefficient）根据物理材质调整**：
    次级光线的发散速度由光线类型决定。
    *   **理想镜面反射（Perfect Specular / Mirror）**：光线不发散。
        $$ray\_size\_coef_{secondary} = ray\_size\_coef_{primary}$$
    *   **光泽反射（Glossy Reflection）**：基于表面粗糙度（Roughness $lpha$）。粗糙度越大，反射圆锥角越大。
        $$ray\_size\_coef_{secondary} = 	an(lpha)$$
    *   **环境光遮蔽（AO）或 漫反射（Diffuse GI）**：漫反射属于半球形发射，通常可以用极大的发散角来近似（例如一个覆盖立体角很大的圆锥）。
        $$ray\_size\_coef_{secondary} = 大数值 (例如 0.5 \sim 1.0)$$
    
**数学结论**：当发射高粗糙度的次级光线时，`ray_size_coef` 会非常大。代入核心判定公式后，$tc_{max} \cdot ray\_size\_coef$ 这一项会快速膨胀，使得光线在弹射出很短的距离后，不等式就瞬间成立。系统会直接读取粗糙层级的父节点（也就是低分辨率的、合并了颜色的“大体素”），极大地节约了计算资源，这也就巧妙实现了体素数据的“各向同性过滤”与软阴影效果。
