# 附录/方法论补充：VABSDF 核心假设与近似的误差验证方案

在推导限制可见表面的聚合双向散射分布函数（VABSDF）并将其应用于体素化渲染管线时，本研究引入了一系列数学与工程近似。为了严谨量化这些近似对最终物理真实度和渲染质量的影响，本文设计了以下“微观-宏观”两级验证方案。

## 一、 微观数学验证：VABSDF 的核心降维与分解

本部分主要通过离线蒙特卡洛（Monte Carlo）采样，验证推导过程中的核心数学近似误差。

### 1. 分离近似误差 (Splitting Approximation Error)
*   **假设内容**：假设材质响应与可见性在体素内部空间上是不相关的，从而将 VABSDF 的原始连乘积分（公式 3）因子分解为纯材质项（公式 4）和纯可见性项（公式 5）的乘积[cite: 1]。
*   **验证方法**：
    1. 提取单体素内的所有多边形几何。
    2. 使用离线脚本（如 Compute Shader）在多边形表面生成大量随机采样点。
    3. 利用蒙特卡洛积分计算公式 3 的**基准真值 (Ground Truth, GT)**。
    4. 分别独立计算公式 4 和公式 5 的精确积分并相乘。
*   **可视化呈现**：针对代表性体素绘制 **2D 极坐标切片图 (Polar Plot)**。对比 GT 曲线与分离近似曲线，以直观展示去相关性导致的能量缩放或偏移误差。

### 2. 多波瓣离散化误差 (8-Lobe Representation Error)
*   **假设内容**：为避免运行时的微表面遍历，将精确的微平面积分（公式 4）强制坍缩为固定方向的 8 波瓣混合模型（公式 6）[cite: 1]。
*   **验证方法**：
    1. 根据公式 6 计算 8 波瓣模型的解析近似结果。
    2. 将其与通过蒙特卡洛求出的公式 4 的精确结果进行对比。
*   **可视化呈现**：
    *   **2D 极坐标切片图**：在分离近似的图表中加入公式 6 的曲线。用以展示 8 波瓣模型作为角域低通滤波器（Angular Low-pass Filter）如何抹平高频的材质反射细节（毛刺）。
    *   **3D 球面热力图 (Spherical Heatmap)**：将出射方向域映射至球面，使用假彩色标示 $|GT - Approximation|$ 的绝对误差，高亮误差最大的角度区域。
    *   **误差收敛曲线**：绘制以“体素内三角形数量”为横轴、“$L^2$ 距离”为纵轴的折线图，论证当几何复杂度增加时该离散化误差的收敛性。

### 3. 可见性公式的降维误差 (Visibility $\omega_o$ Simplification)
*   **假设内容**：为保证预计算数据的存储可行性，在公式 9 的推导中消去了包含出射方向 $\omega_o$ 的项[cite: 1]。
*   **验证方法**：构造特定的测试几何（例如相互靠近的平行平面）[cite: 1]。手动或通过采样计算理论聚合可见性，并与去除了 $\omega_o$ 后的近似公式计算值进行比对。
*   **可视化呈现**：使用**数据对比表格**列出在特定视角下的数值差异[cite: 1]。展示该近似在多数情况下的合理性及潜在的失效边界。

---

## 二、 宏观工程验证：代理几何与系统级假设

本部分主要通过端到端的渲染对比与性能分析，验证工程层面的数据结构优化与几何代理假设。

### 4. 球谐函数截断误差 (Low-order SH Truncation)
*   **假设内容**：使用 $l=0$ 和 $l=2$ 的低阶球谐函数来近似评估体素内的投影面积与可见性投影面积，舍弃了更高频的几何形状细节[cite: 1]。
*   **验证方法**：计算复杂多边形在球面各个方向上的精确投影面积，并与 2 阶球谐函数的重建结果计算误差。
*   **可视化呈现**：制作**球谐近似对比图**[cite: 1]。左侧展示投影面积真值的球面热力图，右侧展示 2 阶近似的热力图，并在底部标注全局 $L^2$ 误差，证明其对宏观面积评估的准确性。

### 5. 双面材质与波瓣减半假设 (Double-sided Material Assumption)
*   **假设内容**：假设材质属性为双面，使得统计数据在空间中呈现中心对称，从而仅需存储上半球的 4 个波瓣[cite: 1]。
*   **验证方法**：执行消融实验（Ablation Study）。实现两个渲染版本：无假设的完整 8 波瓣版本，以及存储优化的 4 波瓣版本[cite: 1]。
*   **可视化呈现**：
    *   **图像质量**：提供包含全局光照的端到端**渲染对比图**及 **FLIP 图像误差图**。
    *   **性能指标**：提供**显存占用与预计算时间对比表**[cite: 1]。论证在维持极低 FLIP 误差的前提下，该假设能够削减 50% 的材质参数显存开销。

### 6. 截断椭球代理的失效边界 (Truncated Ellipsoid Failure Cases)
*   **假设内容**：假设使用单一的截断椭球即可紧密包裹并代理体素内的所有不规则表面[cite: 1]。
*   **验证方法**：使用极低的体素网格分辨率（例如 $64^3$）渲染包含复杂交界面的场景（如室内墙角、家具边缘）[cite: 1]。
*   **可视化呈现**：
    *   展示失效情况下的**渲染对比图**（观察异常的半透明漏光或混合瑕疵）[cite: 1]。
    *   提供透射率 $\tau(\omega_o)$ 和聚合可见性 $\hat{V}(\omega_o)$ 的**伪彩色可视化图 (False-color maps)**[cite: 1]。通过标注数值异常的交界区域，客观呈现代理几何精度不足所导致的系统性误差，确立算法的使用边界。
